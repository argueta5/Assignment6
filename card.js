class Card {
    constructor(rank, suit) {
        this.rank = rank; // Initialize the rank of the card (e.g., '2', '3', 'A')
        this.suit = suit; // Initialize the suit of the card (e.g., 'Hearts', 'Spades')
    }
}